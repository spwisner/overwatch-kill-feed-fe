import React from 'react'
import styled from '@emotion/styled'

const HeaderContainer = styled.div`
	color: red;
`

function Header() {
	return <HeaderContainer>Overwatch Kill Feed</HeaderContainer>
}

export default Header
